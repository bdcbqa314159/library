#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

// Bit Parallel Algorithms
/*
Hamming Distances
int hamming(string a, string b){
    int d = 0;
    for (int i = 0; i<k; i++){
        if (a[i]!=b[i]) d++;
    }
    return d++;
}
Fastest way:
int hamming(int a, int b){
    return __builtin_popcount(a^b);
}

// Counting subgrids
int count 0;
for (int i = 0; i<n; i++){
    if (color[a][i] == 1 && color[b][i] == 1)
        count++;
}

Fastest way:
int count = (row[a]&row[b]).count();

// Reachibility in Graphs
reach[x][x] = 1;
for (auto u:adj[x]){
    reach[x]|=reach[u];
}
*/
// Amortized Analysis
/*
// Two Pointers Method (2sum problem)
// Nearest Smaller Elements (using stacks)
// Sliding Window Minimum (using queues)
*/

// Finding Minimum Values
/*
// Finding Minimum Values
Ternary search
Convex functions
Minimzing sums

*/

int main()
{

    return 0;
}