#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

// Basic Topics
/*
// Trie Structure

int trie[N][A];

// Dynamic Programming
Example: Longest Common Subsequence
lcs(i,j) = lcs(i-1,j-1)+1 if x[i]==y[i];
lcs(i,j) = max(lcs(i,j-1), lcs(i-1,j)) else

Example: Edit Distances
edit(a,b) = min (edit(a, b-1)+1, edit(a-1, b)+1, edit(a-1,b-1)+cost(a,b);
cost(a,b) = if x[a]==y[b];
cost(a,b) = 0 else;
*/

// String Hashing
/*
// Polynomial Hashing
Applications:
    Pattern Matching
    Distinct Substrings
    Minimal Rotation

Collision and Parameters
*/

// Z-Algorithm
/*
// Constructing the Z-Array

// Applications
    Pattern Matching
    Finding Borders
*/

// Suffix Arrays
/*
// Prefix Doubling Method
// Finding Patterns
// LCP Arrays

*/

int main()
{

    return 0;
}