#include <iostream>
#include <string>
#include <algorithm>

using namespace std;

// Geometric Techniques

/*
// Complex Numbers
// Some possible coordinate type definitions:
typedef long long C;
typedef long double C;

=> typedef complex<C> P;
#define X real();
#define Y imag();

P p = {4,2};
cout << p.X<<" "<<p.Y<<"\n";

P v = {3,1};
P u = {2,2};
P s = u+v;
cout << s.X <<" "<<s.Y<<"\n";

P a = {4,2};
P b = {3,-1};
cout << abs(b-a) <<"\n";

P v = {4,2};
cout << arg(v)<<"\n";
v *= polar(1.0, 0.5);
cout << arg(v)<<"\n";

// Points and Lines
cross product (2D) x1y2-y1x2

P a = {4,2};
P b = {1,2};
C p = (conj(a)*b).Y;

Usages of cross products:
-Testing Point Location
-Line Segment Intersection
-Distance from a Point to a Line

// Polygon Area
=>Shoelace formula (check it in the book.)

Pick's Theorem: a different way to calculate the area of a polygone.
All the vertices having integer coordinates.
a number of points inside the polygon and b is the number of integer points on the boundary of the polygon.
Area = a + b/2 -1;

// Distance functions
Usuals: L2 & L1.
*/

// Sweep Line Algorithms
/*
// Intersection Points

// Closest Pair Problem

// Convex Hull Problem

*/

int main()
{

    return 0;
}